//Arrays are object in javascropt
//Arrays are list of object and the object can be completely different.
let selectedColor = ['red', 'blue'];
selectedColor[2]= 'green';
selectedColor[3] = 1;
console.log(selectedColor.length);
console.log(selectedColor);